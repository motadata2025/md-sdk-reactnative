# Exclude test scaffolding from the published tarball (kept in git, not shipped).
# The `files` allowlist in package.json still governs inclusion; these prune within it.
**/__tests__/**
**/__mocks__/**
**/__snapshots__/**
**/*.test.ts
**/*.test.tsx
**/*.test.js
**/*.spec.ts
**/*.spec.tsx
